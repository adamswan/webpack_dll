import $ from "jquery";
import * as _ from "lodash";
import * as Vue from "vue";
import * as Echarts from "echarts";

console.log("打印jquery", $);
console.log("打印lodash", _);
console.log("打印vue", Vue);
console.log("打印echarts", Echarts);
